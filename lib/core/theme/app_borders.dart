import 'package:flutter/material.dart';

abstract final class AppBorders {
  static const thin = BorderSide(width: 1);
  static const medium = BorderSide(width: 1.5);
  static const thick = BorderSide(width: 2);
}
