abstract final class AppDuration {
  
  static const fast = Duration(milliseconds: 150);

  static const normal = Duration(milliseconds: 300);

  static const slow = Duration(milliseconds: 500);

  static const readerGlow = Duration(milliseconds: 800);

  static const splash = Duration(seconds: 2);

  
  static const frame = Duration(milliseconds: 20);

  static const micro = Duration(milliseconds: 50);

  
  static const focus = Duration(milliseconds: 200);

  static const quickShift = Duration(milliseconds: 350);

  
  static const switchOut = Duration(milliseconds: 400);

  static const dock = Duration(milliseconds: 650);

  static const reveal = Duration(milliseconds: 700);

  static const stageEnter = Duration(milliseconds: 800);

  static const entrance = Duration(milliseconds: 1000);

  static const longEntrance = Duration(milliseconds: 1200);

  
  static const dialogPulse = Duration(milliseconds: 1500);

  static const logo = Duration(milliseconds: 1800);

  static const longPause = Duration(milliseconds: 2500);

  static const breathe = Duration(seconds: 2);

  static const ambient = Duration(seconds: 3);

  static const cue = Duration(seconds: 4);

  static const aurora = Duration(seconds: 5);

  static const auroraGold = Duration(seconds: 6);

  static const auroraFade = Duration(seconds: 7);
}
